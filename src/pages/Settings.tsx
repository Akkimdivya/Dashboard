import React from 'react';

const Settings: React.FC = () => {
  return (
    <div>
      <h2>Settings Page</h2>
      {/* Add your settings page content here */}
    </div>
  );
};

export default Settings;

// Add this line if the file doesn't have any imports or exports
export {}; 
